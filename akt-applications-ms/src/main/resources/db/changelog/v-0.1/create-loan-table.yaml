databaseChangeLog:
  - changeSet:
      id: create-loan-table
      author: tural
      changes:
        - createTable:
            columns:
              - column:
                  autoIncrement: true
                  constraints:
                    primaryKey: true
                    primaryKeyName: loan_application_pkey
                  name: id
                  type: bigint
              - column:
                  name: step
                  type: varchar(50)
              - column:
                  name: agent_username
                  type: varchar(255)
              - column:
                  name: customer_Fin
                  type: varchar(10)
            tableName: loan_application
